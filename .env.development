# ── Desarrollo local ──────────────────────────────────────────────────────────
#
# BACKEND_URL   → usado por vite.config.ts (Node.js) como destino del proxy.
#                 NO lleva prefijo VITE_ porque NO debe exponerse al navegador.
#
# VITE_API_BASE_URL → NO definir en desarrollo.
#                     apiClient.ts usará '/api/backend', que Vite redirige
#                     al proxy definido arriba. Así evitamos problemas de CORS.
#
BACKEND_URL=http://localhost:8080
